name: Security Baseline

on:
  pull_request:
  push:
    branches:
      - main
      - goal-*

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  dependency-scan:
    name: Dependency scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: apps/web/package-lock.json
      - name: Install web dependencies
        working-directory: apps/web
        run: npm ci
      - name: Block high severity web advisories
        working-directory: apps/web
        run: npm audit --audit-level=high
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install API audit tooling
        working-directory: services/api
        run: |
          python -m pip install -e '.[dev]'
          python -m pip install pip-audit
          python -m pip freeze --exclude-editable > audit-requirements.txt
      - name: Block known vulnerable API dependencies
        working-directory: services/api
        run: pip-audit --strict -r audit-requirements.txt

  secret-scan:
    name: Secret scan
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Scan for committed secret patterns
        shell: pwsh
        run: ./scripts/scan-secrets.ps1
