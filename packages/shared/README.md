# Shared Package

Planned home for shared contracts or generated types if the web and API need a
common package boundary.

Do not put domain logic here unless both app surfaces genuinely consume it.

